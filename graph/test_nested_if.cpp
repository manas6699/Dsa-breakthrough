#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a = 1;
    int n = 5;
    for (int i = 0; i < n; i++)
    {
        if (n > 0)
        {
            if (a > 0)
            {
                 cout << "n is greatr than 0 & a is reater than 0" << endl;
                 break;
            }
            
           
        }
        
    }

    cout << "for test 2 : " << endl;
    for (int i = 0; i < n; i++)
    {
        if ((n > 0) && (a > 0))
        {
            
                 cout << "n is greatr than 0 & a is reater than 0" << endl;
                 break;
            
            
           
        }
        
    }
    

    return 0;
}